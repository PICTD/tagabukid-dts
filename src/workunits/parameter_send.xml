<workunit>
    <invokers>
        <invoker type="send:form" caption="Send Parameters" action="init" target="popup" > </invoker>
    </invokers>
     <code>

        import com.rameses.rcp.common.*
        import com.rameses.rcp.annotations.*
        import com.rameses.osiris2.client.*
        import com.rameses.osiris2.common.*

        class SendParametersController
        {
            @Binding
            def binding;
         
            def entity;
            
            void init(){
                getSendtypeHandler()
            }
         
            void refresh() {
                if(binding) binding.refresh();
            } 
         
            def getSendtypeHandler(){
                    return InvokerUtil.lookupOpener(entity.sendtype+':form',[
                            entity: entity
                        ])
            }
        }  

    </code>
    <pages>
        <page template="tagabukid.dts.SendParametersPage" />
    </pages>
</workunit>