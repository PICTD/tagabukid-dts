<workunit>
    <invokers>
        <invoker type="localoutgoing:form" caption="Outgoing Parameters Local" action="init" target="popup" > </invoker>
    </invokers>
     <code>

        import com.rameses.rcp.common.*
        import com.rameses.rcp.annotations.*
        import com.rameses.osiris2.client.*
        import com.rameses.osiris2.common.*

        class OutgoingParametersLocalController
        {
            @Binding
            def binding;
         
            def entity;
            
            void init(){
              entity.org = [:];
            }
         
            void refresh() {
                if(binding) binding.refresh();
            } 
         
            def getLookupOrg(){
                return Inv.lookupOpener('userorg:lookup',[
                        onselect :{
                            entity.org.OrgUnitId = it.organizationid
                            entity.org.name = it.orgname
                            entity.org.code = it.orgcode
                        },
                ])
            }
           
        }  

    </code>
    <pages>
        <page template="tagabukid.dts.OutgoingLocalPage" />
    </pages>
</workunit>